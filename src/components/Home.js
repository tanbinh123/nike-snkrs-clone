import styled from 'styled-components';
import Menu from './Menu';

const Home = (props) => {
    return(
        <Container>
            <Menu />
        </Container>
    )
}

const Container = styled.div``;

export default Home;